# Write your MySQL query statement below
SELECT u.name ,COALESCE(SUM(r.distance),0) as travelled_distance FROM Users u LEFT JOIN rides r ON u.id=r.user_id GROUP BY user_id ORDER BY travelled_distance DESC,name ASC;